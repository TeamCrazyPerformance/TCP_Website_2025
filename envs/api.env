# API 백엔드 환경변수 설정 (예시)
# 프로덕션 환경에서는 실제 값으로 변경하고 깃허브에 커밋하지 마세요!

# JWT 설정
# JWT 시크릿 키 (강력한 랜덤 문자열 사용 권장)
# 생성 방법: openssl rand -base64 64
JWT_SECRET=

# 비밀번호 해싱 설정
# bcrypt salt rounds (높을수록 안전하지만 느림, 10~12 이상 권장)
BCRYPT_SALT_ROUNDS=12

# 서버 설정
PORT=
NODE_ENV=production

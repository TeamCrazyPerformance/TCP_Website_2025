# INTERNAL / AUTO-GENERATED by set_env.sh
PORT=3000
NODE_ENV=production
BCRYPT_SALT_ROUNDS=12
JWT_SECRET=

# The pipeline URL, service token, and timeouts are injected directly by
# docker-compose.yml from root .env. MySQL and Gemini values never enter api.env.
